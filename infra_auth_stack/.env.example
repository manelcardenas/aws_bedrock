# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# Auth Stack Configuration
# Copy this file to .env and fill in your actual values
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# JWT Secret - MUST be the same for both auth_lambda and proxy_lambda
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(32))"
JWT_SECRET=CHANGE-THIS-TO-RANDOM-SECRET-IN-PRODUCTION

# JWT Token Expiration (in hours)
JWT_EXPIRATION_HOURS=24

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# Existing API Endpoints (from your deployed stacks)
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# Image API Configuration
# Get this from: cd ../infra_images && cdk deploy (check outputs)
IMAGE_API_URL=https://your-image-api-id.execute-api.us-west-2.amazonaws.com/prod/image

# Get API key with: aws apigateway get-api-key --api-key <KEY_ID> --include-value --region us-west-2
IMAGE_API_KEY=YOUR_IMAGE_API_KEY_HERE

# Text API Configuration  
# Get this from: cd ../infra && cdk deploy (check outputs)
TEXT_API_URL=https://your-text-api-id.execute-api.eu-west-3.amazonaws.com/prod/text

# Get API key with: aws apigateway get-api-key --api-key <KEY_ID> --include-value --region eu-west-3
TEXT_API_KEY=YOUR_TEXT_API_KEY_HERE
